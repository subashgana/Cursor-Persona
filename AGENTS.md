# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Cursor-Persona`) is currently an empty/blank project with only a `README.md`. There is no application code, no dependency manifests, no services, and no build/test/lint tooling configured yet.

### Current state

- **Languages/Frameworks:** None configured
- **Package managers:** None
- **Services:** None
- **Tests:** None
- **Lint:** None
- **Build:** None

### Notes for future agents

- Once application code is added, update this section with startup, test, lint, and build instructions.
- The update script (`SetupVmEnvironment`) is a no-op placeholder (`echo "No dependencies to install"`). Replace it once a dependency manifest (e.g. `package.json`, `requirements.txt`) is committed.
